---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 9d2199a010da227b7f5130c5e523a5b4_e88092fca99911f190de525400461939
    ReservedCode1: RURO51pUDGyt8ZqjbHJXvoiJjeaeXOFbvd2w6r61EgEdjdUyZYJGJpq48BDCxe5UoxluRgnI10HdHCUeMp0NYqEZUSqIW6FHXnq2vZTVU8dfj2N/eo8sYGlhJypZY69VsPugot/h4JA1BPM2+SP5n0fRNuw1OHP8ja4CNTKNTN0AYQdKpNYKPaZr2SE=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 9d2199a010da227b7f5130c5e523a5b4_e88092fca99911f190de525400461939
    ReservedCode2: RURO51pUDGyt8ZqjbHJXvoiJjeaeXOFbvd2w6r61EgEdjdUyZYJGJpq48BDCxe5UoxluRgnI10HdHCUeMp0NYqEZUSqIW6FHXnq2vZTVU8dfj2N/eo8sYGlhJypZY69VsPugot/h4JA1BPM2+SP5n0fRNuw1OHP8ja4CNTKNTN0AYQdKpNYKPaZr2SE=
---

# RatCount 自动化测试补充报告

> 生成日期：2026-09-06 ｜ 项目：F:\static\RatCount（Next.js 16 App Router + React 19 + TS + Drizzle ORM + Turso/libSQL，14 张表，金额统一 *_cents 分存储，账户余额实时计算不落库，业务表均带 ledger_id 做账本隔离）
> 测试框架：Node.js 原生 `node:test` + `tsx` 运行器（与项目既有 unit/money 测试一致）

## 一、项目解读要点

- 技术栈：Next.js 16 App Router + Turbopack + React 19 + TS 5.9 + Tailwind 4 + Drizzle 0.44 + Turso/libSQL（`DATABASE_MODE` 支持 turso / libsql / file 双部署）。
- 数据库：14 张表（users / ledgers / ledger_members / settings / currencies / accounts / categories / tags / projects / transactions / transaction_tags / audit_logs / balances / recurring_plans）；金额统一分存储；转账不计收支；业务表全带 `ledger_id` 隔离；C/U/D 操作全部经 `withAudit` 写审计。
- 权限模型：Auth.js v5 JWT + `requireUser`（登录守卫）+ `requireLedgerAccess(minRole)`（账本级角色：owner>editor>viewer）+ 归属校验 `eq(x.ledgerId, ledger.id)`。
- Server Actions：app/actions 下 transactions / meta / accounts / ledgers / recurring / auth(register/login) / settings / import / export 等，均走库层校验 + 审计。
- 核心 lib：scope（账本隔离守卫）、money（分/元换算）、queries（事务/报表 query）、audit（审计写入）、validators（zod 校验，金额大数字符串、白名单 id 过滤）、recurring（周期日期推进）、rate-limit（注册限流）、pagination / sql-utils / i18n。

## 二、现有测试情况（任务开始前）

| 文件 | 用例数 | 说明 |
|---|---|---|
| test/unit.test.ts | 18 | 验证码 / 限流 / 余额计算 / 周期日期推进（既有） |
| test/money.test.ts | 5 | 金额分元换算（既有） |
| test/check-schema.ts / check-security.ts | - | 独立校验脚本（非 node:test 用例） |
| test/*-test-cases.md + report.md | - | 五类用例设计文档 v1.2/1.3（设计层，含浏览器回归纪要） |

**发现的关键问题**：`package.json` 的 `"test": "node --test"` 无法运行任何 TypeScript 测试——Node 原生 ESM 解析不支持无扩展名 import（`lib/money`、`helpers/db-fixture`）、目录 import（`lib/db`）及 `@/` 别名。即此前 `npm test` 跑不起来。

## 三、新增测试清单（6 个文件 + 1 个夹具，共 84 条用例）

| 新增文件 | 用例数 | 覆盖点 |
|---|---|---|
| test/helpers/db-fixture.ts | - | DB 测试夹具：注入 env + 动态加载 lib/db + 14 表 DDL + 建两用户/两账本/账户/分类/标签/项目/流水/标签关联/余额快照，返回 ID 供断言 |
| test/unit-validators.test.ts | 18 | zod 校验器：交易入参（类型/金额/日期/备注长度/转账目标）/筛选白名单（type/id/q 截断/金额/日期）/注册（邮箱/密码边界 6-72/昵称 1-30）/登录必填 |
| test/unit-pagination.test.ts | 13 | 分页计算（page/pageSize 边界、越界钳制）、offset 计算、日期格式化/解析往返、SQL `IN` 占位、枚举常量、报表 period 解析 |
| test/ui-rendering.test.ts | 11 | 报表工具函数（fmt/getPeriodLabel/typeKey）、金额格式化（分→元、千分位、负数）、验证码 SVG/公式、账本 cookie 写入解析 |
| test/functional.test.ts | 19 | Server Actions 行为：默认账本自动建（注册建账本+默认分类）、分类/标签/项目/账户 CRUD 全流程、流水记账/编辑/删除后余额实时计算、转账不计收支、账本+成员角色、周期计划推进、报表聚合 |
| test/security.test.ts | 12 | 权限越权（viewer 写拒绝、跨账本访问拒绝、非 owner 删账本拒绝）、账本隔离（同 id 不同账本数据不可见）、SQL/参数注入（恶意 q/id/minAmount/type 一律字面量处理、白名单 schema 拦截、DROP 尝试不清库）、验证码防重放（过期/错误拒绝）、审计记录落库 |
| test/ue-ux.test.ts | 11 | 用户体验流程：分页翻页窗口、期初/实时/快照对账提示、验证码输入容错、限流注册、金额输入校验（非法输入实时回退）、报表期间联动、周期推进跨月/跨年、重复操作幂等性 |

**合计新增 84 条**（单测 31 + 功能 19 + UI 11 + UE 11 + 安全 12）。

## 四、测试运行结果

- 命令：`npm test`（已修复为 `tsx --test "test/*.test.ts"`）
- 结果：**107 条用例全部通过（pass 107 / fail 0）**，耗时约 3.3s
  - 新增 6 文件 84 条全过
  - 既有 unit.test.ts + money.test.ts 23 条全过

## 五、本次修复记录

| 文件 | 修复内容 |
|---|---|
| package.json | `"test": "node --test"` → `"test": "tsx --test \"test/*.test.ts\""`：使 `npm test` 真实可运行（原生 node 无法解析无扩展名/别名/目录 import）；glob 限定 *.test.ts 避免误跑独立脚本 |
| lib/db/client.ts | 真实项目 Bug：`wrapTx` 的 Proxy 包装事务对象未绑定 `commit`/`rollback` 的 `this`，file 模式事务结束抛 "Cannot read private member #database" 且事务无法正常关闭遗留写锁；已改为对 `commit`/`rollback` 返回 `fn.bind(target)` |

## 六、遗留与建议

- check-schema.ts / check-security.ts 为独立校验脚本，未纳入 node:test（保持原样）。
- 冒烟级 E2E（真实 Next 路由 + 浏览器）建议后续引入 Playwright，与 test-cases.md/report.md 中的浏览器回归纪要衔接。
*（内容由AI生成，仅供参考）*
